// Sigil-Logic/clafer#33: arithmetic over a clafer is not closed; it has no
// declaration-position rendering in Alloy and no chocosolver encoding, so it
// is rejected with a positioned semantic error.
y -> integer
x -> (y + 1)

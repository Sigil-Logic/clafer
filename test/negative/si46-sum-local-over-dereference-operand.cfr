// Sigil-Logic/clafer#46: a local declared over a dereference ranges over values,
// not clafers, so `sum i` is rejected with a positioned semantic error instead
// of aborting the Alloy generator (HOARDE Codex, PR #48 Cycle 3).
abstract N ->> integer
n1 : N = 5
[ all i : N.dref | sum i > 0 ]
